PORT=4000
JWT_SECRET=change-this-to-a-long-random-string
JWT_EXPIRES_IN=7d
# Optional: override the SQLite file location
# DB_PATH=./data/community4you.db
# Optional: comma-separated list of allowed frontend origins (production CORS).
# Leave unset in development to allow all origins.
# CLIENT_ORIGIN=https://community4you.netlify.app
