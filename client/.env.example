# Base URL of the deployed backend API (no trailing slash), e.g. Render:
# VITE_API_URL=https://community4you-api.onrender.com
#
# Leave this UNSET in local development — Vite proxies "/api" to localhost:4000.
VITE_API_URL=
